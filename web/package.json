{
  "name": "bridge-web",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
